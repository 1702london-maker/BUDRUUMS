import { metadataFor } from "@/app/seo";

export const metadata = metadataFor("founderBlueprint");

export default function FounderBlueprintLayout({ children }: { children: React.ReactNode }) {
  return children;
}
