import { metadataFor } from "@/app/seo";

export const metadata = metadataFor("socialMediaMarketing");

export default function SocialMediaMarketingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
