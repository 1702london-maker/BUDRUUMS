import { metadataFor } from "@/app/seo";

export const metadata = metadataFor("startupConsultancy");

export default function StartupConsultancyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
