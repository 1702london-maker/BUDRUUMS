import { metadataFor } from "@/app/seo";

export const metadata = metadataFor("mentorship");

export default function MentorshipLayout({ children }: { children: React.ReactNode }) {
  return children;
}
