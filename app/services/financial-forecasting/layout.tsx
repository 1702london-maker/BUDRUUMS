import { metadataFor } from "@/app/seo";

export const metadata = metadataFor("financialForecasting");

export default function FinancialForecastingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
