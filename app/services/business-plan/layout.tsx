import { metadataFor } from "@/app/seo";

export const metadata = metadataFor("businessPlan");

export default function BusinessPlanLayout({ children }: { children: React.ReactNode }) {
  return children;
}
