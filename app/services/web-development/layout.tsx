import { metadataFor } from "@/app/seo";

export const metadata = metadataFor("webDevelopment");

export default function WebDevelopmentLayout({ children }: { children: React.ReactNode }) {
  return children;
}
