import { metadataFor } from "@/app/seo";

export const metadata = metadataFor("innovatorVisa");

export default function InnovatorVisaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
