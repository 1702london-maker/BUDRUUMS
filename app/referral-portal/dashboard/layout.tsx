import { metadataFor } from "@/app/seo";
export const metadata = metadataFor("referralPortal");
export default function ReferralPortalDashboardLayout({ children }: { children: React.ReactNode }) {
  return children;
}
