import { metadataFor } from "@/app/seo";

export const metadata = metadataFor("bookingSuccess");

export default function BookingSuccessLayout({ children }: { children: React.ReactNode }) {
  return children;
}
