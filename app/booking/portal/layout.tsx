import { metadataFor } from "@/app/seo";
export const metadata = metadataFor("booking");
export default function BookingPortalLayout({ children }: { children: React.ReactNode }) {
  return children;
}
