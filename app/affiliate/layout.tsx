import { metadataFor } from "@/app/seo";

export const metadata = metadataFor("affiliate");

export default function AffiliateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
