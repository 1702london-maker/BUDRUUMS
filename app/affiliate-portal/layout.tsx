import { metadataFor } from "@/app/seo";

export const metadata = metadataFor("affiliatePortal");

export default function AffiliatePortalLayout({ children }: { children: React.ReactNode }) {
  return children;
}
